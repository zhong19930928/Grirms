package com.yunhu.yhshxc.listener;

public interface OnMultiChoiceConfirmListener {
	
	/**
	 * 点击确定按钮 
	 * @param chooseStatus当前选中的状态数组
	 */
      public void OnConfirm(boolean chooseStatus[]);
      /**
       * 模糊搜索
       * @param fuzzyContent 模糊搜索的内容
       */
      public void fuzzySearch (String fuzzyContent);
}
